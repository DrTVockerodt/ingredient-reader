# ingredient-reader
A small scale portfolio project dedicated to image recognition, which will look at reading food labels to determine whether or not a food is vegan.

Themes: Natural language processing (NLP), image recognition, artificial neural networks (ANNs),
convolutional neural networks (CNNs), data analysis, data modelling,
machine learning (ML), statistics, multilayer perception (MLP).
